# QuickType Repository Instructions

- Every time code is changed, update the `quicktype-build` meta tag in `index.html` to the current local timestamp, including the UTC offset.
- Treat the build timestamp update as part of the same change, verification, commit, and push.
