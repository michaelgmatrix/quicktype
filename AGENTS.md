# QuickType Repository Instructions

- Whenever `index.html` is changed, update its `quicktype-build` meta tag to the current local timestamp, including the UTC offset.
- Treat the build timestamp update as part of the same `index.html` change, verification, commit, and push.
